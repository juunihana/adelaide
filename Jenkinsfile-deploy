pipeline {
    agent any
    stages {
        stage ('deploy') {
            steps {
                sh 'docker login -u admin -p admin http://localhost:9201'
                sh 'docker pull localhost:9201/repository/docker/adelaide_api:0.0.1'
                sh 'docker-compose up -d'
                sh 'docker logout'
            }
        }
    }
}